<script lang="ts">
  import Button from '$lib/components/Button.svelte'; import CodeBlock from
  '$lib/components/CodeBlock.svelte';
</script>

# Button

The `Button` component is a simple, versatile button that can be used for various actions.

---

## 🔧 Usage

<CodeBlock language="svelte">
{`
<script>
  import { Button } from 'your-library';
</script>

<Button on:click={() => alert('Button clicked!')}>
  Click me
</Button>
`}
</CodeBlock>

---

## 🧾 Props

| Prop       | Type    | Default | Description                  |
| ---------- | ------- | ------- | ---------------------------- |
| `disabled` | boolean | false   | Disables the button          |
| `href`     | string  | -       | Renders as `<a>` if provided |

---

## 💡 Tip

Use `href` when you want the button to behave like a link.
